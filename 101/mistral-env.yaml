---
version: "2.0"

env_exp:
  tasks:
    task1:
      with-items: item in ["{% for ip in env().get('service_ips', {}).get('ceph_mon_ctlplane_node_ips', []) %}{{ ip }}{% endfor %}"]
      action: std.echo output=<% $.item %>
